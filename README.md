# map_container
I use map container in this program.
<br>
Add map.cpp file.
<br>
In this code snippet, we demonstrate the usage of a map in C++. We create a map that associates strings (names) with integers (ages). We insert key-value pairs into the map, iterate through the map to display its contents, and perform various operations such as checking if the map is empty, counting occurrences of a key, finding a key, erasing a key-value pair, and updating values. Finally, we clear the map and display its contents again to show that it has been emptied.
